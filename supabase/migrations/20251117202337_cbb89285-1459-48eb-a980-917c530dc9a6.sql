-- Fix security warnings by replacing the function (not dropping)
CREATE OR REPLACE FUNCTION update_article_clusters_updated_at()
RETURNS TRIGGER 
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path = public
AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$;