# =============================================================================
# ENVIRONMENT CONFIGURATION - DO NOT COMMIT REAL VALUES
# =============================================================================
# This file serves as a template. The actual .env is managed by Lovable Cloud.
# Copy this to .env and replace placeholders with real values for local dev.

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Auto-populated by Lovable Cloud)
# -----------------------------------------------------------------------------
VITE_SUPABASE_PROJECT_ID=your_project_id_here
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_here
VITE_SUPABASE_URL=https://your-project.supabase.co

# -----------------------------------------------------------------------------
# SECURITY SECRETS (Set in Supabase Edge Function secrets)
# -----------------------------------------------------------------------------
# CRON_SECRET=<generate-strong-random-string>
# CREDENTIALS_ENCRYPTION_KEY=<generate-32-char-random-string>
# ALLOWED_ORIGINS=https://your-app.lovable.app,https://your-custom-domain.com

# -----------------------------------------------------------------------------
# WEBHOOK SECRETS (Set in Supabase Edge Function secrets)
# -----------------------------------------------------------------------------
# ACTBLUE_WEBHOOK_SECRET=<from-actblue-dashboard>
# ATTRIBUTION_WEBHOOK_SECRET=<generate-strong-random-string>

# -----------------------------------------------------------------------------
# EXTERNAL API KEYS (Set in Supabase Edge Function secrets)
# -----------------------------------------------------------------------------
# META_CONVERSIONS_API_TOKEN=<from-meta-business-manager>
# RESEND_API_KEY=<from-resend-dashboard>
# CONGRESS_API_KEY=<from-congress-api>
# REDDIT_CLIENT_ID=<from-reddit-developer>
# REDDIT_CLIENT_SECRET=<from-reddit-developer>

# -----------------------------------------------------------------------------
# NOTES
# -----------------------------------------------------------------------------
# - Never commit the actual .env file to version control
# - All secrets should be managed through Supabase Edge Function secrets
# - Rotate keys regularly, especially after any potential exposure
# - Use strong, randomly generated values for all secrets
