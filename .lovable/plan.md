

# Update VA-6 Downloadable Results

## Overview
Replace the existing VA-6 download file with the newly uploaded memo document.

## Changes

| File | Change |
|------|--------|
| `public/downloads/VA-6_Memo-2.docx` | Replace with content from `user-uploads://VA-6_Memo_1.docx` |

The poll data in `src/data/polls/va6-2026.ts` already matches the numbers in this memo, so no data changes are needed. Only the downloadable file needs to be swapped.

